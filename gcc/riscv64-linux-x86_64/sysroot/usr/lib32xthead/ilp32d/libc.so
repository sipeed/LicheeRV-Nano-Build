/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-littleriscv)
GROUP ( /lib32xthead/ilp32d/libc.so.6 /usr/lib32xthead/ilp32d/libc_nonshared.a  AS_NEEDED ( /lib/ld-linux-riscv32xthead-ilp32d.so.1 ) )
